export class Member {
  id: string;
  name: string;
  avatar: object;

  constructor(id: string, name: string, avatar: object) {
    this.id = id;
    this.name = name;
    this.avatar = avatar;
  }
}
